#include <bits/stdc++.h>
using namespace std;

int arr[300001];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t, n;
    bool test1, test2, test;

    cin >> t;
    while (t--) {
        cin >> n;

        for (int i = 0; i < n; i++) {
            cin >> arr[i];
        }

        test = false;
        int val = 0;
        for (int i = 0; i < n; i++) {

            if ()
            for (int j = 0; j < i; j++) {
                if ()
            }
        }

        if (test) cout << "Yes\n";
        else cout << "No\n";
    }

    return 0;
}